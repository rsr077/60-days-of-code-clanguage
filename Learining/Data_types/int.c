#include<stdio.h>
int main(){
    // INteger vlaue with positive data.
    int a =9;
    // Integer vlaue with negative data.
    int b =-9;
    // U or u is used for Unsigned int in C
    int c =89U;
    // L or l is used for Unsigned int in c.
    long int d= 99998L;
    printf("Integer value with positive dat :%d\n",a);
    printf("Integer value with negative data :%d\n",b);
      printf("Integer value with unsigend int data :%u\n",c);
       printf("Integer value with an long int data  :%ld\n",d);
       return 0;
}